package hunters.ds;

class BasicQueue<T> {
  private T[] queue; 
  private int tail;
  private int head;

  public BasicQueue(int size) {
    this.tail = -1;
    this.head = -1;
    queue = (T[]) new Object[size];
  }

  public BasicQueue() {
    this(1000);
  }
}
